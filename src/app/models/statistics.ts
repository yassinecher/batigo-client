export class Statistics {
    totalIncomes: number;
    totalExpenses: number;
    balance: number;
  
    constructor(totalIncomes: number, totalExpenses: number, balance: number) {
      this.totalIncomes = totalIncomes;
      this.totalExpenses = totalExpenses;
      this.balance = balance;
    }
  }
  